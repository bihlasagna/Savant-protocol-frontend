import './index.scss';

const Searchbar = () => {
  return (
    <input
      type="text"
      className="search-bar bg-inputcontrol rounded text-base"
      placeholder="Search"
    />
  );
};

export default Searchbar;
